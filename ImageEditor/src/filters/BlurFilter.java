package filters;

import utils.ImageMatrix;

public class BlurFilter extends Filter {

	@Override
	public ImageMatrix apply(ImageMatrix image) {
		
		// Implement the blur filter logic here
        // Apply the blur transformation to the image
        // Return the modified image
        
        // Example:
        // int width = image.getWidth();
        // int height = image.getHeight();
        // // Apply the blur transformation to each pixel in the image
        // for (int i = 0; i < width; i++) {
        //     for (int j = 0; j < height; j++) {
        //         // Apply the blur filter to the pixel at (i, j)
        //         // Modify the RGB values of the pixel accordingly
        //     }
        // }
        // return the modified image
		
		
		return image;
	}
	

}
